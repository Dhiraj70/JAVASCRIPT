// 1. create an array of number and take input from the user to add number to this 

let arr = [1, 2, 3, 4, 5, 6, 7];

let a = prompt("Enter a number");
a = Number.parseInt(a);

arr.push(a);

console.log(arr)












// 2. keep adding numbers to the array in 1. unit 0 is added to the array

// 3. filter for number disble by 10 from a given array

// 4. create an array of space of given number

// 5. use reduce of calculate factorial of a given number from an array of first n natural
// number ( n being the number whose factoral needs to be calculeted)